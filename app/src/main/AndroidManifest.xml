<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spragginsdesigns.bibleaistudytool"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Request Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/verse_mind_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/verse_mind_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BibleAIStudyTool"
        tools:targetApi="31">

        <!-- MainActivity declaration with android:exported attribute -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
